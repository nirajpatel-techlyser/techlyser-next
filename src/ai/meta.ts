/**
 * Lightweight AI OS metadata — safe for Admin UI imports.
 * Do not import research/writer engines from here (avoids Prisma browser-stub crashes).
 */

export const AI_GROWTH_OS = {
  name: "Techlyser AI Growth OS",
  phase: 6,
  status: "seo-geo" as const,
  version: "0.6.0",
} as const;
